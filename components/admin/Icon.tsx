/* Compact brand mark for the collapsed Payload admin nav. */
export const Icon = () => {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/images/logo/logo1.png"
      alt="Mongolian Outdoors"
      width={30}
      height={30}
      style={{ borderRadius: "50%", objectFit: "cover" }}
    />
  );
};

export default Icon;
